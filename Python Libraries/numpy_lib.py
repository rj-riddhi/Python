# Numpy is the python library for scientific computing in pyton

# Advatages: less memory, fast and convinient

import numpy as np

a = np.array([(1, 2, 3), (4, 5, 6)])
print(f"Two dimensional array is: {a}")
